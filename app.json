{
  "name": "icadx",
  "description": "ICADX - SOFTWARE DE GESTÃO DE UBS",
  "keywords": ["icadx"],
  "scripts": {
    "dokku": {
      "predeploy": "bundle exec rake assets:precompile",
      "postdeploy": "bundle exec rake db:migrate && bundle exec rake seed:migrate"
    }
  }
}
