<%= render 'application/page_header', model: Vaccination %>
<div class="card">
  <div class="card-status bg-transparent"></div>
  <div class="card-header p-4">
    <div class="col-sm-12 col-lg-12">
      <div class="form-row">
        <div class="col-sm-12 col-lg-6 p-0">
          <%= render 'application/pagy_count', list: @pagy %>
        </div>
        <div class="col-sm-12 col-lg-6 p-0">
          <%= search_form_for @query do |f| %>
          <%= f.search_field :search_cont, class: 'form-control', placeholder: t('helpers.search') %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="card-body table-responsive p-0 m-0">
    <table class="table card-table table-vcenter text-nowrap table-hover">
      <thead>
      <tr>
                  <th><%= Vaccination.human_attribute_name :uuid %></th>
                  <th><%= Vaccination.human_attribute_name :tp_cds_origin %></th>
                  <th><%= Vaccination.human_attribute_name :company %></th>
                  <th><%= Vaccination.human_attribute_name :slug %></th>
                <th><%= t('helpers.labels.actions') %></th>
      </tr>
      </thead>
      <tbody>
      <% @vaccinations.each do |vaccination| %>
        <tr>
                      <td><%= vaccination.uuid %></td>
                      <td><%= vaccination.tp_cds_origin %></td>
                      <td><%= vaccination.company_id %></td>
                      <td><%= vaccination.slug %></td>
                    <td class="text-center">
            <%= render 'application/table_links', f: vaccination %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
  <div class="card-footer">
    <%= render 'application/footer_links', url: new_vaccination_path, model: Vaccination %>
  </div>
</div>
